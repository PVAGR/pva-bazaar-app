## Do not put secrets here. This file is a placeholder to document that runtime secrets belong in `backend/.env`.
## Create `backend/.env` from `backend/.env.example` and NEVER commit it.
## Required keys (see backend/.env.example):
## MONGODB_URI=
## JWT_SECRET=
## Placeholder file (no secrets).
## Application secrets belong only in backend/.env (untracked) created from backend/.env.example.
## Do NOT add real credentials here.

