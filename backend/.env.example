PORT=5000
NODE_ENV=development

# REQUIRED (set real values in backend/.env; do not commit that file)
MONGODB_URI=
JWT_SECRET=

# Optional dev settings
# USE_MEMORY_DB=true
# DEV_AUTO_SEED=true

# CORS (adjust for your frontend dev server port)
ALLOWED_ORIGIN=http://localhost:5173
