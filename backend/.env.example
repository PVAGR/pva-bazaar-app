# Backend Environment Variables Template
# Copy this file to .env and fill in your real values
# For Vercel deployment, set these in the Vercel dashboard

# Port (not needed on Vercel serverless, but useful for local development)
PORT=5001

# Environment
NODE_ENV=production

# Database (REQUIRED for production)
MONGODB_URI=<your-production-database-url>

# Security (REQUIRED)
JWT_SECRET=<strong-secret-key-minimum-32-characters>

# Admin Login Credentials (REQUIRED for admin panel access)
ADMIN_USERNAME=<your-admin-username>
ADMIN_PASSWORD=<your-admin-password>

# Blockchain
ETHEREUM_RPC_URL=https://mainnet.base.org
ADMIN_WALLET_PUBLIC=0x463ace850a958e768618361e352fe9efe31d5d0e

# Solana payout rituals (optional; devnet by default)
SOLANA_CLUSTER=devnet
# Destination wallet for ritual payouts (Phantom / Solana address)
SOLANA_TREASURY_WALLET=
# Optional custom RPC endpoint (otherwise cluster default endpoint is used)
SOLANA_RPC_URL=
# Admin test payout policy controls
SOLANA_TEST_MAX_USD=5
SOLANA_TEST_MAX_SOL=0.05
SOLANA_TEST_REQUIRE_ALLOWLIST=false
SOLANA_TEST_WALLET_ALLOWLIST=

# Hot wallet for direct SOL transfers (server-side signing)
# NEVER commit this value. Set it ONLY in Vercel → Project → Settings → Environment Variables.
# Supports two formats:
#   1. Phantom JSON export: open Phantom → Settings → Export Private Key → copy the raw bytes array [1,2,...]
#   2. Base58 encoded 64-byte secret key (standard Solana CLI format)
# Fund this wallet with SOL before using direct-transfer. Start on devnet for testing.
SOLANA_HOT_WALLET_PRIVATE_KEY=

# CORS (set to your GitHub Pages domain)
ALLOWED_ORIGIN=https://pvabazaar.org
# Aliases also supported by backend/api/index.js
CORS_ORIGIN=https://pvabazaar.org
FRONTEND_URL=https://pvabazaar.org

# Feature flags
USE_MEMORY_DB=false
USE_VECTOR_DB=false
DEV_AUTO_SEED=false

# Set to 'true' to enable legacy marketplace endpoints (default: false)
LEGACY_MODE=false

# OpenClaw bridge (optional)
# Base URL of your OpenClaw gateway, for example: https://openclaw.yourdomain.com
OPENCLAW_GATEWAY_URL=
# OpenClaw outbound webhook URL that receives PVA Bazaar dispatch events
OPENCLAW_WEBHOOK_URL=
# Optional explicit health URL (defaults to ${OPENCLAW_GATEWAY_URL}/health)
OPENCLAW_HEALTH_URL=
# Optional bearer token used when calling OpenClaw
OPENCLAW_API_KEY=
# Optional shared secret required for POST /api/openclaw/dispatch via X-OpenClaw-Secret header
OPENCLAW_BRIDGE_SECRET=
# Queue worker tuning (for persistent VPS worker runtime)
OPENCLAW_WORKER_NAME=openclaw-queue-dispatcher
OPENCLAW_WORKER_POLL_MS=10000
OPENCLAW_WORKER_BATCH_SIZE=15
OPENCLAW_WORKER_LEASE_MS=45000
OPENCLAW_WORKER_REQUEST_TIMEOUT_MS=12000
OPENCLAW_WORKER_RETRY_BASE_MS=15000
OPENCLAW_WORKER_RETRY_MAX_MS=300000
OPENCLAW_WORKER_MAX_RETRIES=12
# Multi-worker launcher count (used by npm run openclaw:workers)
OPENCLAW_WORKER_COUNT=1

# Telegram webhook bridge (optional)
# Use /api/openclaw/telegram/register-webhook to register this URL with BotFather token.
TELEGRAM_BOT_TOKEN=
TELEGRAM_WEBHOOK_URL=
TELEGRAM_WEBHOOK_SECRET=
# Set true to allow any chat. Keep false in production and use TELEGRAM_ALLOWED_CHAT_IDS.
TELEGRAM_PUBLIC_MODE=false
TELEGRAM_ALLOWED_CHAT_IDS=
# Optional default recipient for /email when the command omits a recipient.
OPENCLAW_TELEGRAM_DEFAULT_EMAIL_TO=
OPENCLAW_CHAT_TIMEOUT_MS=12000

# Direct Ollama fallback used by Telegram webhook when OpenClaw is queued/unavailable.
OLLAMA_BASE_URL=
OLLAMA_MODEL=llama3.1
OLLAMA_TEMPERATURE=0.35
OLLAMA_TIMEOUT_MS=20000
# Persona memory behavior for Telegram AI self
OPENCLAW_PERSONA_AUTO_LEARN=true
OPENCLAW_PERSONA_CONTEXT_LIMIT=10
OPENCLAW_PERSONA_PROFILE_ID=default

# Marketplace syndication connectors (optional)
# Configure webhook endpoints if you want automatic listing dispatch
# during item registration/retry to external channels.
FACEBOOK_MARKETPLACE_WEBHOOK_URL=
FACEBOOK_MARKETPLACE_WEBHOOK_TOKEN=
ETSY_LISTING_WEBHOOK_URL=
ETSY_LISTING_WEBHOOK_TOKEN=
EBAY_LISTING_WEBHOOK_URL=
EBAY_LISTING_WEBHOOK_TOKEN=

# Cloud Storage Providers (optional - configure the ones you want to use)
# Cloudinary (for images/videos with CDN)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Pinata IPFS (for decentralized storage)
PINATA_API_KEY=
PINATA_API_SECRET=
PINATA_GATEWAY_URL=https://gateway.pinata.cloud/ipfs

# Registrar -> main app mint sync (optional but recommended)
# Shared secret expected by POST /api/items/provenance/sync-mint via
# X-Registrar-Sync-Secret header.
REGISTRAR_SYNC_SECRET=

# AWS S3 (for scalable cloud storage)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_BUCKET_NAME=
AWS_REGION=us-east-1

# Google Cloud Storage (optional)
GCS_PROJECT_ID=
GCS_BUCKET_NAME=
GCS_KEY_FILE=

# Contracts / explorers (deployment + verification)
CONTRACT_ADDRESS=0xYourProductionContractAddress
ETHERSCAN_API_KEY=
POLYGONSCAN_API_KEY=

# Runtime health checks for deploy orchestrator
BACKEND_BASE_URL=https://api.yourdomain.com
REGISTRAR_BASE_URL=https://registrar.yourdomain.com
PUBLIC_SITE_URL=https://pvabazaar.org
