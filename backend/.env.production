# Local production template (safe for git). Put real values in Vercel or untracked .env.
ALLOWED_ORIGIN="https://pvabazaar.org"
NODE_ENV="production"
JWT_SECRET="<set-in-vercel-or-local-env>"
MONGODB_URI="<set-in-vercel-or-local-env>"
LEGACY_MODE="false"
