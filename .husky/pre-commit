#!/usr/bin/env sh
. "$(dirname "$0")/_/husky.sh"

# Delegate to a Node runner to avoid Linux-only shell utilities (bash/grep) on Windows.
node "$(dirname "$0")/../scripts/precommit/portable-precommit.js"

